package com.auction.client.app;

/** JVM entry point for packaged client JARs (JavaFX requires a non-Application main class). */
public class Launcher {
    public static void main(String[] args) {
        ClientApp.main(args);
    }
}
